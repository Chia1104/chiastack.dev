---
title: UI
description: UI component library
---

`@chiastack/ui` is a React UI component library that provides reusable UI components.

## Installation

<Tabs items={['npm', 'pnpm', 'yarn', 'bun']}>
  <Tab value="npm">
    ```bash
    npm install @chiastack/ui
    ```
  </Tab>
  <Tab value="pnpm">
    ```bash
    pnpm add @chiastack/ui
    ```
  </Tab>
  <Tab value="yarn">
    ```bash
    yarn add @chiastack/ui
    ```
  </Tab>
  <Tab value="bun">
    ```bash
    bun add @chiastack/ui
    ```
  </Tab>
</Tabs>

## Dependencies

This package requires React as a peer dependency:

<Tabs items={['npm', 'pnpm', 'yarn', 'bun']}>
  <Tab value="npm">
    ```bash
    npm install react react-dom
    ```
  </Tab>
  <Tab value="pnpm">
    ```bash
    pnpm add react react-dom
    ```
  </Tab>
  <Tab value="yarn">
    ```bash
    yarn add react react-dom
    ```
  </Tab>
  <Tab value="bun">
    ```bash
    bun add react react-dom
    ```
  </Tab>
</Tabs>

## Usage

```typescript
import { name } from '@chiastack/ui';

console.log(name); // "@chiastack/ui"
```

## Note

This package is currently under development. More components will be available in future versions.

