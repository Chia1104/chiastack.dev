---
title: Services
description: 常用的服務整合
---

`@chiastack/services` 提供了一系列常用的服務整合，包括 CAPTCHA 驗證等。

## 安裝

<Tabs items={['npm', 'pnpm', 'yarn', 'bun']}>
  <Tab value="npm">
    ```bash
    npm install @chiastack/services
    ```
  </Tab>
  <Tab value="pnpm">
    ```bash
    pnpm add @chiastack/services
    ```
  </Tab>
  <Tab value="yarn">
    ```bash
    yarn add @chiastack/services
    ```
  </Tab>
  <Tab value="bun">
    ```bash
    bun add @chiastack/services
    ```
  </Tab>
</Tabs>

## 模組

<Cards>
  <Card title="Captcha" href="/zh/docs/services/captcha" description="CAPTCHA 驗證服務整合" />
</Cards>

## 相依性

此套件需要 `@chiastack/utils` 作為 peer dependency：

<Tabs items={['npm', 'pnpm', 'yarn', 'bun']}>
  <Tab value="npm">
    ```bash
    npm install @chiastack/utils
    ```
  </Tab>
  <Tab value="pnpm">
    ```bash
    pnpm add @chiastack/utils
    ```
  </Tab>
  <Tab value="yarn">
    ```bash
    yarn add @chiastack/utils
    ```
  </Tab>
  <Tab value="bun">
    ```bash
    bun add @chiastack/utils
    ```
  </Tab>
</Tabs>

