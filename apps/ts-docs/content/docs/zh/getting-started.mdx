---
title: 快速開始
description: 開始使用 Chia Stack
---

本指南將幫助您快速開始使用 Chia Stack 套件。

## 安裝

Chia Stack 中的每個套件都可以獨立安裝和使用。

### Utils

<Tabs items={['npm', 'pnpm', 'yarn', 'bun']}>
  <Tab value="npm">
    ```bash
    npm install @chiastack/utils
    ```
  </Tab>
  <Tab value="pnpm">
    ```bash
    pnpm add @chiastack/utils
    ```
  </Tab>
  <Tab value="yarn">
    ```bash
    yarn add @chiastack/utils
    ```
  </Tab>
  <Tab value="bun">
    ```bash
    bun add @chiastack/utils
    ```
  </Tab>
</Tabs>

### Services

<Tabs items={['npm', 'pnpm', 'yarn', 'bun']}>
  <Tab value="npm">
    ```bash
    npm install @chiastack/services
    ```
  </Tab>
  <Tab value="pnpm">
    ```bash
    pnpm add @chiastack/services
    ```
  </Tab>
  <Tab value="yarn">
    ```bash
    yarn add @chiastack/services
    ```
  </Tab>
  <Tab value="bun">
    ```bash
    bun add @chiastack/services
    ```
  </Tab>
</Tabs>

### UI

<Tabs items={['npm', 'pnpm', 'yarn', 'bun']}>
  <Tab value="npm">
    ```bash
    npm install @chiastack/ui
    ```
  </Tab>
  <Tab value="pnpm">
    ```bash
    pnpm add @chiastack/ui
    ```
  </Tab>
  <Tab value="yarn">
    ```bash
    yarn add @chiastack/ui
    ```
  </Tab>
  <Tab value="bun">
    ```bash
    bun add @chiastack/ui
    ```
  </Tab>
</Tabs>

## 使用範例

### Utils 範例

```typescript
import { delay, tryCatch } from '@chiastack/utils';

// 延遲執行
await delay(1000); // 等待 1 秒

// 安全的錯誤處理
const result = await tryCatch(fetch('/api/data'));
if (result.error) {
  console.error(result.error);
} else {
  console.log(result.data);
}
```

### Services 範例

```typescript
import { captchaSiteverify } from '@chiastack/services/captcha';

// 驗證 CAPTCHA
const response = await captchaSiteverify(request, {
  provider: 'cloudflare-turnstile',
  captchaSecretKey: process.env.CAPTCHA_SECRET_KEY!,
});
```

## 下一步

- 查看 [Utils 文件](/zh/docs/utils) 了解所有可用的工具函式
- 查看 [Services 文件](/zh/docs/services) 了解可用的服務整合
- 查看 [UI 文件](/zh/docs/ui) 了解可用的元件
